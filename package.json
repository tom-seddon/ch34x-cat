{
  "name": "ch341x-cat",
  "version": "1.0.0",
  "description": "cat utility for CH341x USB serial adapter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo tsc... && tsc -p ./tsconfig.json && node ./.build/main.js",
    "lint": "tslint --project ./tsconfig.json"
  },
  "author": "Tom Seddon",
  "license": "GPL-2.0-or-later",
  "dependencies": {
    "@types/argparse": "^1.0.34",
    "@types/usb": "^1.1.6",
    "argparse": "^1.0.10",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1",
    "usb": "^1.3.2"
  }
}